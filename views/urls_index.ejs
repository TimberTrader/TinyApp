<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
    <!-- Add EJS code here -->
  <body>
    <header>
      <% include ./partials/_header %>
    </header>

    <main>
      <a href='/new'>Create New TinyURL</a>
      <h4>My URLs</h4>
          <ul>
          <% for (let shortName in urls) { %>
          <li><%= shortName %>:   <%= urls[shortName].longURL %>
            <form class = "form-inline" method= "POST" action= "/urls/<%= shortName %>/delete ">
              <div class="form-group mb-2">
                <button type = "submit" class = "btn btn-primary">delete</button></form>
                <button type = "submit" class = "btn btn-primary">edit</button></form>
              </div>

              </form> 
            </li>
            <% }; %>
          </ul>
    </main>

  </body>
</html>

